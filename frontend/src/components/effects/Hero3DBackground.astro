<div class="fixed inset-0 z-0 pointer-events-none overflow-hidden" aria-hidden="true">
  <!-- Gradiente base oscuro -->
  <div class="absolute inset-0 bg-[#060606]"></div>
  
  <!-- Orbes de luz animados — mezcla amber (marca) + cyan/blue (logo) -->
  <div class="absolute top-1/4 -left-1/4 w-[600px] h-[600px] rounded-full bg-amber-500/5 blur-[120px] animate-pulse-slow"></div>
  <div class="absolute bottom-1/4 -right-1/4 w-[500px] h-[500px] rounded-full bg-cyan-400/5 blur-[100px] animate-pulse-slower"></div>
  <div class="absolute top-1/3 right-1/3 w-[400px] h-[400px] rounded-full bg-blue-600/4 blur-[90px] animate-pulse-blue"></div>
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-amber-400/3 blur-[150px] animate-pulse-slowest"></div>
  
  <!-- Grid líneas sutiles -->
  <div class="absolute inset-0 opacity-[0.03]" style="background-image: linear-gradient(rgba(6,182,212,0.3) 1px, transparent 1px), linear-gradient(90deg, rgba(251,191,36,0.3) 1px, transparent 1px); background-size: 60px 60px;"></div>

  <!-- Partículas decorativas (estilo logo) -->
  <div class="absolute inset-0 opacity-[0.15]">
    <div class="particle particle-1"></div>
    <div class="particle particle-2"></div>
    <div class="particle particle-3"></div>
    <div class="particle particle-4"></div>
    <div class="particle particle-5"></div>
  </div>
</div>

<style>
  @keyframes pulse-slow {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 0.8; }
  }
  @keyframes pulse-slower {
    0%, 100% { transform: scale(1); opacity: 0.3; }
    50% { transform: scale(1.15); opacity: 0.6; }
  }
  @keyframes pulse-slowest {
    0%, 100% { transform: translateX(-50%) translateY(-50%) scale(1); opacity: 0.2; }
    50% { transform: translateX(-50%) translateY(-50%) scale(1.05); opacity: 0.4; }
  }
  @keyframes pulse-blue {
    0%, 100% { transform: scale(1); opacity: 0.3; }
    50% { transform: scale(1.2); opacity: 0.6; }
  }
  @keyframes float-particle {
    0%, 100% { transform: translateY(0) translateX(0); opacity: 0; }
    25% { opacity: 0.6; }
    50% { opacity: 0.3; }
    75% { opacity: 0.5; }
    100% { transform: translateY(-100vh) translateX(50px); opacity: 0; }
  }
  .animate-pulse-slow { animation: pulse-slow 8s ease-in-out infinite; }
  .animate-pulse-slower { animation: pulse-slower 12s ease-in-out infinite; }
  .animate-pulse-slowest { animation: pulse-slowest 15s ease-in-out infinite; }
  .animate-pulse-blue { animation: pulse-blue 10s ease-in-out infinite; }

  .particle {
    position: absolute;
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: #06b6d4;
    box-shadow: 0 0 6px 2px rgba(6, 182, 212, 0.3);
  }
  .particle-1 { left: 20%; top: 80%; animation: float-particle 12s ease-in-out infinite; animation-delay: 0s; }
  .particle-2 { left: 50%; top: 90%; animation: float-particle 15s ease-in-out infinite; animation-delay: 2s; background: #fbbf24; box-shadow: 0 0 6px 2px rgba(251, 191, 36, 0.3); }
  .particle-3 { left: 75%; top: 85%; animation: float-particle 10s ease-in-out infinite; animation-delay: 4s; background: #3b82f6; box-shadow: 0 0 6px 2px rgba(59, 130, 246, 0.3); }
  .particle-4 { left: 35%; top: 95%; animation: float-particle 18s ease-in-out infinite; animation-delay: 1s; }
  .particle-5 { left: 65%; top: 75%; animation: float-particle 14s ease-in-out infinite; animation-delay: 3s; background: #fbbf24; box-shadow: 0 0 6px 2px rgba(251, 191, 36, 0.3); }
</style>
